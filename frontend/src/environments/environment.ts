export const environment = {
  production: true,
  // In a real production deployment, this would point to your production API URL
  // e.g., 'https://api.luxestay.com/api'
  apiUrl: 'http://localhost:8082/api'
};
